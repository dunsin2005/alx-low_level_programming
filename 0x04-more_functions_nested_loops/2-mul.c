#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: the number to be multiplied with b
 * @b: the number to be multiplied with a
 * Return: Always 0
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
