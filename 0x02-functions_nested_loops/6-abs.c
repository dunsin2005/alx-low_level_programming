#include "main.h"
/**
 * _abs - prints absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
