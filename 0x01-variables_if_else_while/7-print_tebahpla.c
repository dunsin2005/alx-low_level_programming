#include <stdio.h>
/**
 * main - prints alphabety in reverse
 * Return: Always 0
 */
int main(void)
{
	char x;

	x = 'z';
	while
		(x >= 'a')
		{
			putchar (x);
			x--;
		}
	putchar ('\n');
	return (0);
}
